# Ghostty Terminal Configuration
# ~/.config/ghostty/config
# Author: Gökhan Arkan (@gokhanarkan)

# Font settings
font-family = "JetBrains Mono Nerd Font"
font-size = 14
font-weight = "regular"
font-weight-bold = "bold"

# Disable font ligatures if you prefer
# font-feature = -liga
# font-feature = -calt

# Theme and colors
theme = "catppuccin-mocha"

# Alternative themes you can try:
# theme = "gruvbox-dark"
# theme = "tokyo-night"
# theme = "dracula"
# theme = "nord"
# theme = "one-dark"

# Background settings
background-opacity = 0.95
background-blur-radius = 10

# Window settings
window-width = 120
window-height = 40
window-padding-x = 10
window-padding-y = 10
window-save-state = "always"
window-theme = "system"

# Cursor settings
cursor-style = "block"
cursor-opacity = 1.0
cursor-color = "#61afef"

# Scrollback
scrollback-limit = 10000

# Bell
audible-bell = false
visual-bell = false

# Mouse
mouse-hide-while-typing = true
copy-on-select = false

# Shell integration
shell-integration = true
shell-integration-features = "cursor,sudo,title"

# Performance
renderer = "auto"
unfocused-split-opacity = 0.7

# Key bindings (vim-style where appropriate)
keybind = "cmd+c=copy_to_clipboard"
keybind = "cmd+v=paste_from_clipboard"
keybind = "cmd+t=new_tab"
keybind = "cmd+w=close_surface"
keybind = "cmd+n=new_window"
keybind = "cmd+shift+n=new_window"
keybind = "cmd+d=new_split:right"
keybind = "cmd+shift+d=new_split:down"
keybind = "cmd+shift+w=close_all_windows"
keybind = "cmd+1=goto_tab:1"
keybind = "cmd+2=goto_tab:2"
keybind = "cmd+3=goto_tab:3"
keybind = "cmd+4=goto_tab:4"
keybind = "cmd+5=goto_tab:5"
keybind = "cmd+6=goto_tab:6"
keybind = "cmd+7=goto_tab:7"
keybind = "cmd+8=goto_tab:8"
keybind = "cmd+9=goto_tab:9"
keybind = "cmd+shift+left_bracket=previous_tab"
keybind = "cmd+shift+right_bracket=next_tab"
keybind = "cmd+equal=increase_font_size:1"
keybind = "cmd+minus=decrease_font_size:1"
keybind = "cmd+zero=reset_font_size"
keybind = "cmd+shift+c=inspector:toggle"

# Search
keybind = "cmd+f=search"
keybind = "cmd+g=search:next"
keybind = "cmd+shift+g=search:previous"

# Custom color overrides (optional)
# You can override specific colors if needed
# foreground = "#abb2bf"
# background = "#282c34"
# cursor-color = "#61afef"

# Selection colors
# selection-foreground = "#ffffff"
# selection-background = "#3e4451"

# URL handling
click-repeat-interval = 500
mouse-scroll-multiplier = 3

# Terminal features
auto-update = "check"
confirm-close-surface = false
quit-after-last-window-closed = true

# Working directory
working-directory = "inherit"

# Custom commands (optional)
# command = "tmux"
# command = "fish"

# Advanced settings
minimum-contrast = 1.1
bold-is-bright = false

# Experimental features
# gpu-memory-limit = "auto"
