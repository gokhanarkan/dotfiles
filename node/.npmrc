# .npmrc - NPM Configuration
# Author: Gökhan Arkan (@gokhanarkan)
# Since you prefer pnpm, this is mostly for fallback scenarios

# Registry configuration
registry=https://registry.npmjs.org/

# Security settings
audit-level=moderate

# Performance
prefer-offline=true
prefer-dedupe=true

# Logging
loglevel=warn

# Save exact versions (no ^ or ~)
save-exact=true

# Disable package-lock.json since using pnpm
package-lock=false

# Fund messages
fund=false

# Update notifier
update-notifier=false

# Progress indicator
progress=true

# Init defaults
init-author-name=Gökhan Arkan
init-author-email=gokhanarkan@gmail.com
init-license=MIT
init-version=1.0.0

# Cache settings
cache-max=2592000000  # 30 days in ms
cache-min=10

# Optional: Corporate proxy settings (uncomment if needed)
# proxy=http://proxy.company.com:8080
# https-proxy=http://proxy.company.com:8080
# noproxy=localhost,127.0.0.1,*.company.com

# Optional: Alternative registry for scoped packages
# @gokhanarkan:registry=https://npm.pkg.github.com
